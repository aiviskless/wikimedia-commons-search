import Alert from '@material-ui/lab/Alert';
import React from 'react';

const OutOfService = () => (
  <Alert severity="error">Wikimedia Commons is out of service - try again later</Alert>
);

export default OutOfService;
